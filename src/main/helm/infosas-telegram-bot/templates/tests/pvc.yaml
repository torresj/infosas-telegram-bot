apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: infosas-telegram-bot-pvc
spec:
  storageClassName: nfs-storageclass
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
